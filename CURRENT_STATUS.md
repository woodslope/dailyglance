# DailyGlance 当前接续状态

更新时间：2026-09-22（横盘下沿防守发布 b036e1c）

上一阶段完整记录：`docs/history/CURRENT_STATUS_ARCHIVE_2026-08-09.md`

## 当前状态

- 开发与 Pages 发布效率治理已接入：`scripts/check.js` 将多回归分组合并为单进程并采用摘要输出，语法检查改为进程内解析；`scripts/status-smoke.js` 用加载层结束条件替代固定 8 秒等待并支持 `--summary`，`scripts/live-dataflow-smoke.js` 支持摘要输出；`scripts/release-pages.js` 固定 Pages 允许列表、隔离 worktree 和显式 `--push` 门槛。全量检查实测由约 62 秒降至约 52 秒；本地状态 smoke 当前仍受公开行情接口未返回历史数据影响，属于外部数据风险。
- 当前 GitHub Pages 已发布提交 `b036e1c`，应用构建为 `2026-09-21-05`、资源版本为 `20260921-05`，正式策略版本为 `v4.2.42`，波段治理版本 `wave-regime-v7`。远端 `03-decision.js?v=20260921-05`；本次发布 smoke 因本机未安装 Playwright 浏览器未跑，主门禁为已通过的 `--full` 完整回归（303 项）。完整源码仍在本地 `codex/full-project`。
- 波段抄底型非下跌环境「清得太急」已定位为同一病根的三个出口，逐个用只读诊断量化后处理：(1) 首破买入日最低价 → 方案B 确认缓冲已上线（v4.2.40）；(2) 横盘破近端冻结防守位 → 改用箱体下沿防守已上线（v4.2.42，`range.defenseUsesBoxLowerEdge`）；(3) 锁定期信号硬失效「连坐清仓」→ 全样本 97 只 38 事件诊断：信号失效当天从不硬清（100% 先保留 30%），但保留仓 73.7% 在 ≤5 日内被锁定期 priceHardInvalidation 连坐清到 0，其中 13 个（占 38 事件 34%、占被清 46%）事后又收复，属被洗嫌疑——待做候选。诊断工具 `strategy-wave-diagnostic.js` 已支持按环境切分防守位距离/来源、卖后快速重买来回、离场成因按环境、破防守位箱体内位置、信号失效结局追踪。
- 波段抄底型建仓防守位缺陷已修复并上线（v4.2.41）：`03-decision.js` 的箱体支撑分支原用 `Number.isFinite(Number(waveContext?.boxSupport))` 判定，箱体无效时 `boxSupport` 为 `null` 而 `Number(null)===0` 会误通过，导致冻结硬防守位被设成 0（形同虚设、永不触发跌破清仓）。已改为显式要求 `boxSupport` 非 null、为正且低于收盘，否则回落到默认 pivot/signalLow。工作组诊断对照：建仓日到冻结防守位距离 P90 由 100.00% 降至 5.77%，2 日内被清由 28.4% 降至 24.1%。`strategy-wave-diagnostic.js` 已加“按建仓环境切分防守位距离与来源”，实测四环境（下跌/横盘/上涨/过渡）距离中位均在 2–5%、P90 均 ≤6.7%，此前担心的“非下跌环境用远端 120 日 pivot 导致防守位过松”在数据上不成立，主因即上述 null 缺陷。
- `assets/js/03-calculations.js`（原 5423 行）已按职责纯物理拆分为 6 个 `03-*.js`，零行为差异（切割前验证 6 段拼接与原文件字节级一致，`--full` 303 项全过）：`03-calculations.js`(指标+信号判定)、`03-explain.js`(决策解释与展示文案)、`03-summary.js`(新手结论汇总)、`03-wave-regime.js`(趋势环境与波段上下文)、`03-wave-rejection.js`(建仓否决与风险事件保护)、`03-decision.js`(波段治理链与决策合成)。所有源码拼接消费方（`index.html` 6 个 script、`tests/regression.js`、`strategy-formal-baseline/candidate-lab`、`strategy-autopsy(-multi)`、`wave-b-quality-report` 逐文件哈希、`presentation-state` 加载顺序断言、`map.js` 横幅正则支持字母后缀）已同步。routing 前缀 `assets/js/03-` 自动覆盖新文件，无需改路由。
- 波段首破买入日最低价（`fresh_entry_hard_break`）已接入可配置确认缓冲并上线：`freshEntryDownsideFailure.hardBreakConfirmTradingDays=1` 时首破当日不整清，先降到 `30%` 观察仓（`hardBreakPendingPositionCap`）并挂 `pending_hard_break` 锁定，次日收盘仍不收复买入日最低价才确认清仓（`triggered`），收复则解除（`hard_break_recovered`）。该行为是“仓位上限”而非“下限”：只有当天基础决策仍支持 ≥30% 时才真正托住 30%，基础已无支撑的破位日仍会当天清 0。全样本快筛（24 标的 / 182 受影响决策日）状态 `ready_for_product_review`：平均收益 `-0.10pp`、平均最大回撤改善 `0.22pp`、胜率 `+0.27pp`、换手 `-0.6%`、完整交易 `-12`，属“以微小收益换回撤与换手”的取舍型改动，非收益优势主张。
- 修正效率相关的结构性问题两项：`strategy-formal-candidate-lab.js` 的 `configPatch` 改为深合并（浅合并会整体替换 `waveRejectionProtection` 等嵌套子配置而丢失兄弟键）；`strategy-wave-diagnostic.js` 新增只读 `--events` 通用事件出口（按 `status`/`eventType` 过滤导出任意风险事件日，写入 `.local/strategy-reports/strategy-wave-events.md`），以后追因不必再写一次性脚本。`getExitSeverity` 裁掉数组中永不可达的 `L7/L8`（分支逻辑与行为不变）。
- 四个正式策略继续共用 `0% / 30% / 50% / 80%` 四档仓位和统一 `B/S` 契约；当前生产细则以 `docs/strategy/STRATEGY_DECISION_RULES.md` 为准。
- 核心宽基已退出个股仓位链路：四套策略的个股仓位只由个股信号积分、离场/预警、个股趋势资格、结构防守、冲高回落保护和波段三趋势治理决定；风险评分仅保留为辅助诊断数据，不改变仓位、`B/S` 或清仓。结论面板首行改为“市场背景”，只说明宽基环境。指数路径保留原新增风险上限（未知/待确认 `0%`、偏弱普通 `30%`、偏弱独立走强 `50%`）。
- 核心宽基的界面用词已统一为“核心宽基环境”（左侧卡片标题、指数结论面板首行、规则文档标题），指数列表角标统一为“核心宽基 / 仅观察”；此前并存的“环境核心”“核心市场环境”“核心宽基市场环境”均已废弃。
- 左侧“核心宽基环境”卡片已收敛为纯浏览卡：只展示环境标签、指数口径说明和三项核心宽基状态，删除常驻的大字号上限数字栏（原“指数新增风险 普通 30% / 独立走强 50%”）；`reason` 只保留普通机会一档。真正生效的上限仍由右侧结论面板在被截断当天说明。本组只改展示，未改任何上限数值。
- 弱市位置直觉已做只读回放核对（三项核心宽基等权，2022-09-20 至 2026-05-14 共 880 日）：偏弱日确实位于局部低位（中心 41 日窗口分位中位 `0.38`，偏强为 `0.68`），但 `+20` 日中位为 `-0.81%`、胜率 `46.7%`，均值 `+1.50%` 在剔除最好 10% 交易日后转为 `-0.65%`，正向样本集中在 2024 年 8—9 月一次行情。指数路径回放（8 指数 × 4 策略）中 `increase-capped` 共 1057 个 bar、`+20` 日胜率 `39.3%`，低于 `open` 的 `48.1%`；`independent` 档只占偏弱 bar 的 `0.09%`（8 个 bar，全部来自 `bz50`）。据此判断现有上限无需放宽，也不重启已 `reject` 的 `weak_market_confirmed_80_v1`。
- 每日结论三行已清除“说了但不生效”的限制描述：删除 `风险评估未限制`、`风险评估未额外下调，市场也未限制本次增加仓位`、`当前基础仓位未超过市场新增风险上限`、`风险评估暂按原值处理`、个股侧逐环境重复的 `不限制个股仓位`、指数侧的 `本次未触发额外截断` 与 `核心宽基环境未限制指数新增风险`，以及 `大盘虽偏好，但…` 及其背后引用废弃标签（`全面多头`/`温和偏多`）的 `isFavorableMarket` 分支。现行原则：限制只在真正生效当天出现，未生效时不写任何限制句；无限制的加仓只说明驱动因素。本组只改展示文案，未改变 `position`、`bsMark`、积分或任何上限数值。9 个标的 × 4 套策略回放 37480 条结论，禁用短语命中 0。
- 回归框架修掉一处会掩盖失败的缺陷：`makeBrowserContext` 此前把宿主 `console` 按引用注入 vm，`watchlist-lifecycle` 覆盖 `console.error` 后，`runTest` 打印 `not ok` 的通道被一并替掉，此后的失败只剩退出码、没有任何输出。现改为每个上下文发独立 console 壳，并加定向回归钉住该边界。同时把 `sector trend refresh deduplicates…` 的冷却断言与套件总耗时解耦（`runTest` 不被 `await`，同步阶段约 58 秒已逼近 60 秒冷却窗口，超过即导致第四次刷新真的重扫、`listCalls` 由 `4` 变 `8`）。修复后全量 288 项通过、分组合计与合并跑一致。
- “板块趋势”仍是独立只读扫描层，行业趋势、概念热点、活跃个股和外部环境推测均不进入核心宽基环境、个股策略、仓位、`B/S` 或收益计算。
- 本地维护边界已进一步收敛：`assets/js/07-refresh-controller.js` 独立承载刷新调度，`assets/js/02-observation-data.js` 独立承载外部环境与板块趋势观察；本次拆分未改变策略参数、历史/实时数据契约或 `B/S` 行为。
- 本地完整源码在 `codex/full-project`；手机精简版完整项目提交为 `3d13823`，GitHub `main` 只保存 Pages 必要部署文件。
- 性能与稳定性治理及本轮刷新/图表修复已随 `347cf4d` 发布到 Pages；本地完整项目中的文档、测试与治理工具仍不在 GitHub Pages 部署包内。
- 本地波段三趋势治理与多周期底部试探已完成代码与定向回归接入：横盘为正式交易环境，下跌/横盘/上涨仓位上限分别为 `30%/50%/80%`，首次建仓建立冻结防守生命周期；个股日线在新的 `B20` 与周线双底候选共振、积分达到 `3` 时可建立 `30%` 试探，普通 `4` 分门槛不变；指数、周线执行路径和其他三套策略不进入该治理层。

## 工作区分层

当前手机精简版提交（`3d13823`）作为已提交对照基线；波段治理与结论文案收敛已合并提交为 `a4203d4`。后续任务继续按以下职责分层，跨层改动必须明确说明：

- **界面调整**：`index.html`、`assets/css/`、`assets/js/01-config-ui.js`、`assets/js/04-render.js`、`assets/js/06-settings.js`、`strategy-inspector.html`、`assets/css/strategy-inspector.css`、`assets/js/strategy-inspector.js`。
- **主应用交互**：`assets/js/05-app.js`、`assets/js/07-refresh-controller.js` 及自选股、设置、刷新生命周期。
- **正式策略配置**：`assets/js/00-strategy-config.js`。该文件独立承载正式策略、信号分数、统一仓位档位和策略版本；普通界面任务不再读取或修改它。
- **生产数据与策略**：`assets/js/02-data.js` 负责核心历史、缓存与实时行情，`assets/js/02-observation-data.js` 负责独立的外部环境与板块趋势观察；策略决策位于 `assets/js/03-calculations.js`、`docs/strategy/STRATEGY_DECISION_RULES.md` 和 `strategy-validation-policy.json`。
- **策略研究与验证**：`scripts/strategy-*.js`、`tests/strategy-*.js`、`tests/regression/strategy-decision.cases.js`、`tests/strategy-baseline-snapshots.json`、`docs/history/strategy/`。
- **协作与工具链**：`AGENTS.md`、`README.md`、`STABILITY_CHECKLIST.md`、`CURRENT_STATUS.md`、`scripts/check.js`、`scripts/sync-version.js`、`version.json` 及相关归档清理。

此前按“归档清理”处理的删除项已随 `a4203d4` 一并提交，不并入 Pages 部署包；只有明确要整理稳定基线时，才单独审核这组差异。上述分层用于日常任务隔离，不代表改动已推送或已物理拆到独立分支。

## 波段策略已诊断特性（勿当 bug 重复挖）

以下均经全样本（97 只 / 11.4 万交易日）只读诊断（工具 `scripts/strategy-wave-diagnostic.js`，含 `--events` 及环境/来回/离场成因/箱体位置/信号失效结局/仓位停留/箱体上沿/分层档位结局多组观测）确认，是策略的设计取舍或稀有事件，非缺陷；未来除非有新样本或新证据，不再作为待修问题反复排查：

- **分层“少升档”是正确设计，非欠仓缺陷。** 每段交易按最高仓位分组：停在 30% 的段占 77%，段内最大浮盈中位仅 `2.38%`、实际收益中位 `-2.30%`（多为走不出趋势的失败低吸）；升到 50%/80% 的段浮盈大（中位 `8.89%`/`11.99%`）且实际收益为正（均值 `+3.79%`/`+5.21%`）。分层把 30% 留给试错、50/80 给已确认趋势，放开加仓只会在“最大浮盈才 2.4%、还亏钱”的烂票上放大亏损。80% 仅 14 段是因为真正的大趋势本就稀少，非逻辑损坏。
- **加仓信号稀疏是低吸策略特性。** 非下跌环境持 30% 停留 10717 日中，仅 `1.9%` 当天有 B6/B11 回踩确认；这是“低吸为主、确认才加码”的本色，不是锁太严。
- **箱体上沿主动卖出激活无收益。** 横盘持仓触及上沿 523 日，未减仓者其后 5 日涨跌近乎掷硬币（下跌 `52.8%`、中位 `-0.23%`），放宽上沿卖出条件不能可靠高抛。
- **W1（偏离MA60>25%过热预警）在上涨环境压低仓位是刻意风控。** 曾试做“上涨健康结构豁免 W1 加仓”候选，快筛 0 触发——因分层加仓还要求当天 B6/B11 回踩，与“过热远离均线”几乎不可能同日共存；候选无效已回滚。
- **信号硬失效清仓非独立病根。** 全样本 38 事件当天从不硬清（100% 先保留 30%），其后被清的 71% 是“真跌破冻结防守位/箱体下沿”、“连坐清仓” 0 次；属防守位破位问题的延续，横盘部分已由 v4.2.42 覆盖。
- **过渡环境谨慎（30% 上限、快清）基本正确。** 2043 个过渡日里仅 `9.6%` 是“箱体已确认差连续天数”的准横盘，其余 81% 方向真不明或箱体未成立，不宜放宽识别或重仓。
- **下跌环境 30% 上限、从不加仓是定位取舍**（控风险），非 bug。若要吃主升浪应使用趋势型/突破型策略，而非改造波段抄底型。

已修并上线的三处真缺陷见版本记录：v4.2.40 方案B（首破买入日最低价确认缓冲）、v4.2.41 建仓防守位 null 修复、v4.2.42 横盘箱体下沿防守。

## 当前风险

- 行情源均为公开接口，无 SLA；线上访问仍可能受到网络重置或 CDN 传播延迟影响，发布结论以 Pages 构建状态、远端提交和部署文件版本共同确认。
- 本地 Python 静态服务在 400ms RTT、750Kbps 且无压缩的模拟网络下，已测冷启动为 14.14 秒，超过 6 秒预算；当前未发布版本在 GitHub Pages 压缩传输下尚未实测，不得将本地无压缩数值当作线上结论。
- 桌面 60 分钟/1724 轮与手机 30 分钟/866 轮长稳均只有强制 GC 后堆增长门禁未通过：分别为 `+40.5%` 与 `+27.6%`。两端 DOM、事件监听器、图表实例和调度器数量稳定，异步刷新与 JSONP 任务均已收敛；堆增长与派生指标缓存、日期索引缓存填充同步出现，仍需在后续专项中区分正常驻留与真正泄漏。
- 个股取消核心宽基限制目前只有代码级证据：全量回归 284 项通过，并用本地缓存回放确认 10 只个股 11150 个决策 bar 的 `marketGate.type` 恒为 `open`（覆盖偏弱/分化/偏强/未知四种环境），八只指数仍正常出现 `increase-capped` 与 `entry-blocked`。尚未做跨样本收益、回撤与换手研究，不得宣称该改动改善或不改善业绩。
- 第二阶段行业板块限制缺少稳定的股票—行业指数映射和历史数据契约，现阶段不得直接混入三项核心宽基投票。
- `wave_expiry_b11_takeover_add_v2` 全量只命中 2 次事件，现有证据只支持已冻结的产品取舍，不构成收益优势证明，也不支持继续扩大例外。
- 已拒绝或冻结的弱市 80%、波段冲高回落扩展、失败短交易再入等候选，不在当前快照继续调参或复活；详细证据见 `docs/history/strategy/`。
- 本轮弱市位置回放只有单一底部样本：3.5 年区间内量级像样的底部反转只有 2024 年 8—9 月一次，因此“偏弱日事后收益”的一切均值口径都受 `n=1` 限制，不得据此宣称弱市适合或不适合建仓，也不构成放宽或收紧指数上限的收益证据。
- 波段三趋势治理已通过定向回归，但现有正式研究入口尚未注册“影子分类、下降压力边界、下降闭环、横盘闭环、上涨闭环、分层退出与重入”六个原子候选；目前不得宣称收益、回撤或跨样本优势，也不得用更新基线快照代替候选研究。
- 多周期底部试探目前只有代码级回归证据；本地历史快照共同截止日早于目标观察窗口，尚未完成 8 月 31 日至 9 月 2 日的真实跨个股回放，不得据此宣称所有个股均能捕捉或已改善收益、回撤与换手。
- `scripts/ui-governance-smoke.js` 目前跑不到结尾，且不在 `scripts/check.js` 默认链内，因此不能把它当作 UI 治理已验证的依据。顶栏触控断言已按产品决定从 `44px` 降到 `36px`（设置、日期导航、图表更新在手机档确为 36px，属有意取舍，已记入 `docs/product/UI_DESIGN_SYSTEM.md`），该关卡已通过；但其后仍有至少两处 `waitForFunction` 超时：`:423` 等手机主图触控平移进入 `state.viewport.mode === 'pan'`，干净 HEAD 只带该断言改动时则卡在 `:490` 的响应式断点生命周期。两处均与触控尺寸无关，连跑三次失败点稳定，且仓库内没有该脚本曾经通过的记录，倾向于自 `a4203d4` 前后就已长期为红。尚未区分是脚本的触控派发方式过时（如改用 Pointer Events、`passive` 监听或平移有额外前置条件）还是产品真有缺陷，需独立排查，不并入文案或策略任务。

## 下一步

- 如继续第二阶段行业板块限制，先补齐稳定映射与历史数据契约，再注册独立候选验证。
- 只有出现新的确认样本、稳定行业相对强弱数据或新的独立案例时，才重启已冻结研究问题。
- 若后续恢复本轮稳定性治理，先为重型派生指标缓存设置独立小上限，并收敛日期索引缓存；再区分“缓存填充后的稳态堆”与“非缓存堆持续增长”。
- 当前 Pages 生产文件已发布，但上述堆增长问题仍未关闭；完整项目的最新未提交改动仍只在本机，`origin` 只保存 Pages 部署所需文件，不能作为完整源码备份。
- 如恢复 UI 治理验证，先单独排查 `scripts/ui-governance-smoke.js` 的两处 `waitForFunction` 超时（`:423` 手机主图触控平移、`:490` 响应式断点生命周期），确认是脚本过时还是产品缺陷后再决定修脚本或修产品；在它能完整跑通之前，不把该脚本的运行结果写成 UI 治理结论。
- 如继续波段治理研究，先为六个原子候选逐个注册正式变体，从影子分类开始做 seed 快筛；任一候选失败即停止，不捆绑调整阈值。注册时必须同时声明 `candidateClass` 与 `primaryGoal`，评估器会在两者矛盾时直接报错。
- 如继续验证本次多周期试探，优先补齐覆盖多个个股的 8 月 31 日至 9 月 2 日历史回放，再评估漏买、误买、换手和退出时点。
