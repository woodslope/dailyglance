# DailyGlance 当前接续状态

更新时间：2026-08-02（`20260802-01` 发布收口）

## 策略定位决策（2026-07-31）

- **波段抄底型正式定位为「低位修复试探」**，不做短线波段大仓。核心职责：在超卖/回踩修复证据出现后以 30% 小仓试探，修复确认后按统一仓位处理，反弹失败或趋势再度转弱时快速退出。
- 短线波段大仓（首笔 50%+、独立加仓/防守/清仓）作为长期储备方向，不在当前生产策略中实现。必须等新增独立交易时段数据、科技样本不再系统性恶化、且有稳定的选股层（行业/板块相对强弱）后，才能注册为独立策略候选并重新评估。
- 以下候选全部冻结，重启条件统一为「新增独立交易时段或新标的，且满足各自候选记录中的额外条件」：
  - 候选 H（长期下跌试探上限与入场固定止损）
  - 候选 I（B15 趋势接管）
  - 候选 J（右侧确认新开仓）
  - 确认加仓至 80%
  - 加仓日近端防守（候选 A）
  - 确认更高低点防守（候选 B）
- 白胖右侧候选研究继续保持暂停；重启前必须先有稳定的行业/板块相对强弱数据源和明确的候选股票范围。

## 当前本地状态

- 本轮将“全面弱势”改为“核心宽基偏弱”，正式接入30%/50%分层增量门禁：普通机会上限30%，标的自身独立走强上限50%；已有高仓不因宽基转弱被动压缩。
- 强离场、冷静期、风险评分、预警和 `W4` 硬上限优先于普通仓位回差；宽基门禁不制造 `S`，个股减仓和清仓不会被门禁阻止。
- 股票统一使用“策略参考仓位”，指数使用“当前风险仓位”；单股历史回放展示信号/执行日期、买入持有基准、超额收益、回撤、样本和执行假设。
- 前端策略健康清单和“观察级/验证级”已删除；成本压力和指数分层只保留为核心策略变更的离线研究工具，不参与每日仓位或普通语义修正的发布准入。
- 应用方向已固定为“日常观察工具，不是量化研究平台”；展示翻译、决策语义修正、核心策略变更分三级验证，详见 `AI_WORKFLOW_NOTES.md`。

- 本地完整项目保留在无远端跟踪的 `codex/full-project` 分支；代码审查修复已提交为 `2c44480`。`main` 仅用于 GitHub Pages 部署。
- 当前构建：`APP_BUILD=2026-08-02-01`、资源参数 `?v=20260802-01`、`SIGNAL_VERSION=v4.2.13`。
- 已发布线上：远端 `main=cf02794`，GitHub 仅保留 `index.html`、`assets/`、`.github/workflows/pages.yml` 和 `.gitignore`。
- 「隔夜外盘主题映射」已上线：东方财富外盘 ETF/个股行情 → 半导体与算力、AI 与云计算、智能电动车三个主题 → A 股可观察概念与代表标的。所有映射仅作只读观察，不参与策略计算。
- 代码审查修复已收口：恢复策略生命周期边界和高危清仓语义，撤回未验证仓位例外，修复 JSONP 取消清理与检查脚本误报通过问题。
- 历史确认日 K 改由 TickFlow 主源提供，策略、仓位、信号与 `B/S` 不变。
- 回测池为 `97` 只股票 + `8` 个指数，共同确认截止 `2026-07-29`；最近本地正式报告为 `.local/strategy-reports/formal-strategy-baseline-20260801T155820Z.json`，只作离线研究证据，不是应用运行依赖。
- 已否决的波段候选（已全部冻结，见上方策略定位决策）：
  - “确认后升至 80%”收益提高但平均最大回撤恶化 `0.9945` 个百分点，三段窗口仅 `1/3`。
  - “加仓日低点 / 确认更高低点防守”均未满足正式风险控制准入；后者回撤仅改善 `0.0226` 个百分点且三段窗口 `0/3`。
  - 候选 I（B15 趋势接管）最高保留 50% 版收益边际 `+0.41` 个百分点，未达到 `0.50` 个百分点准入线。
  - 候选 J（右侧确认新开仓）448 个新增 B，5 日扣费后平均 `-0.12%`，失败率 `56.70%`。
  - 候选 H（长期下跌试探上限）科技样本 `9/9` 收益变差，无样本外验证。
- 只有新增/删除信号、改积分阈值、改变第一次 `B/S` 或交易频率，或需要评价收益优势时，才使用 `--screen -> 105标的正式回放` 流程；展示和决策语义修正不再默认进入该流程。
- 本地波段候选回放已优化：正式 105 标的同条件回放从 `323.50s` 降至 `152.07s`，结果逐字段零漂移；快速筛选约 `48s`。`--progress` 每 10 个标的输出进度，命令行只输出摘要，完整明细留在 `.local/strategy-reports/`。
- **信号冗余已确认，不删除**：消融实验证明 `B12`（零轴上金叉）和 `B14`（平台放量突破）在综合全能型中仓位/调仓/B/S 全部零差异，被同组 `B2`/`B4` 完全覆盖；`B10`（MA20 上穿 MA60）在稳健趋势型中只改变 4 组 B/S。这些信号保留仅用于展示层确认，`scoreGroups` 去重已生效，后续不投入资源优化或删除。

## 当前线上状态

- 线上地址：https://woodslope.github.io/dailyglance/
- 发布源为 `20260802-01 / v4.2.13`，远端 `main` 提交 `cf02794`；`Deploy DailyGlance` run `30709291097` 和 Pages run `30709290637` 均已成功。
- 本地发布前 `status-smoke` 与 `live-dataflow-smoke` 已通过。当前终端连续访问 Pages 均在 `page.goto` 前遭遇 `ERR_CONNECTION_RESET`，本轮线上 smoke 未进入应用断言；这与已记录的外部网络风险一致。
- 波段 B11 双层结构防守、核心宽基门禁保持在线；`B8+B17` 金色 B 规则集保持 `shadow`，页面只显示普通 `B`，不改变仓位、动作、风险、离场或 `B/S`。

## 待办与风险

### 待验证

- TickFlow、腾讯、东方财富和新浪均为公开接口，没有正式 SLA；TickFlow 主源当前已通过真实数据流验证，后续仍需观察可用性和频率限制。
- 当前终端网络路径访问 Pages 仍会偶发 `ERR_CONNECTION_RESET`，但重试后线上 DOM/canvas 和真实数据流 smoke 已成功；这是外部网络可用性风险，不是已确认的应用回归。

## 下一步

- `dailyglance` 保持暂停，不自动刷新缓存、生成基线或运行候选；只有人工明确触发才继续。
- **不再对已冻结的波段大仓/防守候选发起新的实验**，除非满足重启条件（新增独立交易时段或新标的）。
- 若有新的明确策略问题，先检查是否属于「低位修复试探」范围。若是大仓/波段方向的新想法，应先注册为独立候选并确认是否满足重启条件，再决定是否跑 `--screen`；不在同一快照重复调参。
- 白胖候选研究在稳定行业/板块相对强弱数据源就绪之前不重启；不通过手工标签或未经验证的均线过滤替代。
- 建议每 1-2 周检查一次 `obs-*` 真实观察记录，积累 5 个案例后统一审视共性；优先从真实使用问题中生成候选，而非在旧快照上扫描信号。
- 本轮阶段测试和发布已完成；后续新修改仍只在人工说“可以阶段测试”或“准备发布”后再运行页面 smoke。策略语义修正不再默认启动成本和指数分层全量回放。
- 后续若有独立于策略方向的前端改进（如性能、交互、展示），仍按正常开发流程处理，不受策略冻结影响。

## 历史入口

- 最新历史记录：`docs/history/STABLE_VERSION_2026-08-02.md`
- 本轮门禁与复杂度纠偏记录：[docs/history/strategy/STRATEGY_VALIDATION_LOG_2026-08.md](docs/history/strategy/STRATEGY_VALIDATION_LOG_2026-08.md)
- 既有策略候选与否决证据：[docs/history/strategy/STRATEGY_VALIDATION_LOG_2026-07.md](docs/history/strategy/STRATEGY_VALIDATION_LOG_2026-07.md)
- 生产规则：[docs/strategy/STRATEGY_DECISION_RULES.md](docs/strategy/STRATEGY_DECISION_RULES.md)
