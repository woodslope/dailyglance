# DailyGlance AI 协作经验规范

本文只管 AI 协作节奏、执行边界和交付方式；文档阅读入口看 `AGENTS.md`，生产验证和版本号看 `STABILITY_CHECKLIST.md`，当前状态和待办看 `CURRENT_STATUS.md`。

## 核心目标

DailyGlance 的优化必须围绕一个目标：让新手每天看一眼，就能知道当前股票或指数的状态、动作、仓位、风险和失效条件。

不为了堆指标、重构或增加复杂度而偏离这个目标。

## 应用方向与复杂度边界

DailyGlance 是日常观察工具，不是量化研究平台或策略认证系统。生产链只保留一条必要路径：

`K 线与指标 -> 标的信号与风险 -> 核心宽基增量门禁 -> 参考仓位与动作 -> 风险和失效条件`

生产界面和每日决策禁止引入策略健康等级、样本分层、成本压力准入、快照哈希或人工认证状态。这些如果有用，只能留在离线研究中，不参与当天仓位，不阻塞语义明确的产品修正。

改动按以下三级选择验证，不得默认上升一级：

| 改动类型 | 例子 | 足够的验证 |
| --- | --- | --- |
| 展示翻译 | 命名、右侧文案、回放说明 | 语法检查、定向文案/界面回归 |
| 决策语义修正 | 宽基只限制增仓、硬风控不被回差恢复 | 关键仓位路径、`B/S` 语义和固定快照回归；可选做一次聚合健全性检查 |
| 核心策略变更 | 新增/删除信号、改积分阈值、改变第一次 `B/S` 或交易频率、宣传收益优势 | 快速筛选后再做完整样本、时间窗口、成本与必要分层回放 |

对于第二级改动，只要决策契约明确、关键路径通过且没有非预期 `B/S`，就不得因为某个成本场景或指数分层收益未达阈值而长期挂起。当一项功能需要向用户解释研究流程才能说明它的价值时，默认先删减而不是再加一层状态。

## 决策校验原则

AI 不是简单执行器。用户提出删文件、跳测试、改策略、改数据边界、移动结构或改变流程时，AI 必须先判断这个决定是否符合轻应用目标和当前风险：

- 正确且低风险：直接执行，保持最小改动。
- 方向正确但方式过重：改用更省时间、更少文件、更低维护成本的方法。
- 可能损坏生产、数据、策略或未来接续：明确指出风险，给出更安全方案，再执行可逆部分。
- 明显不正确：直接说明不建议这样做，并给出替代方案；不能因为用户催促就盲从。

判断标准永远是：是否节省时间、是否减少维护成本、是否保护当前线上稳定、是否服务“新手每天看一眼”的核心目标。

## 问答与执行边界

用户在问“为什么 / 是不是 / 规则是否如此 / 该不该 / 要不要沉淀 / 流程是否失效”时，默认处于解释与审计阶段。AI 只能核对规则、解释原因、列出判断和建议；不得改代码、改规则、改技能、跑测试或执行 Git / 发布操作。

只有用户明确说“修复 / 改掉 / 执行 / 去做 / 更新”并且目标已经清楚时，才进入执行阶段。进入执行阶段前，AI 仍要用一句话说明将改什么、为什么改、验证到哪一级；涉及数据契约、策略语义、右侧结论、技能、小白经验或流程规则时，必须先给出最小审计结论和影响面。

如果用户只是提出一个可能的规则或流程改法，AI 必须先判断它是规则缺失、规则冲突、已有规则执行失败，还是一次性偏好；不要把用户的临时判断直接写成长期规范。只有确认确有可复用缺口时，才把最小规则写入项目文档或经验库。

## 人工问题确认协议

用户报告界面问题时，AI 先用一句话复述自己理解的错误；如果描述和截图足够明确，复述后直接处理，不等待用户再次确认。只有触发路径、期望状态或受影响入口不明确时，才停下来请用户确认。推荐格式：

```text
我理解的问题是：在 [触发路径] 下，[实际表现]，但期望应该是 [期望表现]。我先按这个问题处理；如果理解有偏，你再纠正我。
```

如果用户描述里包含截图、相对方向、快速移动、拖拽或状态变化，复述时把“触发路径、当前状态、期望状态、可能受影响的入口”说清楚。如果无法判断，应先提出一个最小复现动作让人工确认，不要擅自扩写成另一个问题。

## 开发与测试节奏

DailyGlance 当前稳定期采用“小步修复 + 人工探索 + 阶段测试”的节奏：

1. 功能或交互改动完成后，先做必要的轻量确认，确保改动没有明显语法、加载或入口错误。
2. 人工先按真实使用路径探索界面，优先发现肉眼可见的状态错位、交互断裂和文案不一致。
3. 人工发现明显问题后，AI 先复述确认问题，再优先进入修复；修复明显问题后继续给人工检查相邻交互。
4. 当人工检查暂时找不出新问题时，AI 主动提醒进入阶段测试，把 `CURRENT_STATUS.md` 的未关闭待办、相关 smoke 清单和必要回归合并验证。
5. 阶段测试通过后，再更新稳定记录或给出上线前结论。

这个流程不是跳过测试。它只是把“完整 smoke / 批量回归”放到明显问题修完、人工探索暂时收敛之后，避免每发现一个前端细节就反复跑整套测试；但修复本身仍应有最小确认，不能无验证收口。

## 两阶段执行

**开发迭代期**

- 目标是快速修正用户正在看的可见问题。
- 只读必要入口文档和相关源码；不为每个小问题反复扩展阅读所有领域文档。
- 不默认跑完整 smoke、线上验证、全量回归或多入口浏览器复验。
- `scripts/status-smoke.js`、`scripts/live-dataflow-smoke.js`、Playwright 浏览器检查和人工桌面视觉复验都属于完整 smoke。未收到用户明确的“可以阶段测试”或“准备发布”指令前，禁止把它们作为“额外本地证据”提前执行。
- 开发期的必要确认只能覆盖当前改动本身，例如语法检查、定向回归或策略链回放；它们不能被表述为 smoke 或发布验证。
- 只有新增/删除信号、修改积分阈值、改变第一次 `B/S` 或交易频率，以及需要评价收益优势时，才进入完整策略回放。语义修正按关键场景与快照回归收口，不默认扩展成本和分层研究。
- 不默认每个小修都更新稳定版本说明；只在交付说明里简短记录改动和未测风险。
- 不默认做小白经验沉淀、经验审核、PR/tag/CI/监控设计或文档重组。
- 只有出现明确待后续阶段覆盖的风险，才写入 `CURRENT_STATUS.md` 的“待办与风险”；普通“未跑全量测试”不单独开待测项。

**阶段测试 / 发布收口期**

- 触发条件是用户说“人工看完了 / 暂时没新问题 / 可以阶段测试 / 准备发布 / 准备开新对话”。
- 这时再集中检索 `CURRENT_STATUS.md` 的未关闭待办，运行必要回归和 smoke，补稳定记录，整理新对话接续信息；未触发前不得自行提前进入本阶段。
- 若生产代码已准备发布，再按 `STABILITY_CHECKLIST.md` 和 `README.md` 的最小 GitHub Pages 范围处理发布验证。

如果用户没有明确进入阶段测试或发布收口，AI 不应把“记录、测试、复盘、经验沉淀”全部插入每一次开发循环。

## 策略与展示边界

不要重新造第二套策略引擎。生产策略链路、`B/S`、仓位、信号状态和候选池规则以 `docs/strategy/STRATEGY_DECISION_RULES.md` 为准。

右侧结论、关键依据、新手解释、名称展示和页面布局都属于展示层，默认只能翻译既有 `meta`、`decision`、风险和信号状态。展示层优化必须让新手读到：

1. 当前状态
2. 当前动作
3. 当前仓位（股票为策略参考仓位，指数为风险仓位）
4. 风险来源或防守重点
5. 当前判断的失效条件
6. 为什么现在不能买、为什么还能拿或为什么只观察
7. 接下来怎么防守

禁止把观察信号写成买卖承诺，禁止使用“必涨”“稳赚”“抄底成功”等表达。

## 桌面端优先

当前应用是 Web 端量化终端，电脑端是主要使用场景。

移动端只保留“请使用电脑端打开”的门禁提示，不做完整交易终端适配。

涉及视觉复核时，优先检查桌面端右侧面板、图表区、左侧导航和设置弹窗。

## 临时文件与交付记录

不要默认生成长期备份目录。确实需要对照时，只为高风险生产代码或关键文档做临时 before/after，对照完成并把结论写入正式文档后删除临时文件。不要把备份、缓存、报告或协作记忆长期藏在 `.local/`、`.workbuddy/` 或其他隐藏目录里。

阶段测试、发布收口、新对话接续或影响长期规则时，更新稳定记录或相关领域文档，让下一次 AI 能知道：

- 这轮做了什么
- 验证了什么
- 哪些生产行为没有改变
- 下一步为什么值得做

稳定记录优先按日期切分，一天一份或一轮一份，避免单文件无限追加。同一天只有一轮稳定记录时使用 `STABLE_VERSION_YYYY-MM-DD.md`；同一天出现多轮发布或收口时，后续记录使用 `STABLE_VERSION_YYYY-MM-DD-NN.md`，`NN` 按当日顺序递增。当前接续短入口始终是 `CURRENT_STATUS.md`。

## 下一步建议规则

每轮最后如需给下一步建议，必须围绕产品核心目标。

建议要能回答：它是否让新手更容易理解状态、动作、仓位、风险和失效条件。

除非确实必要，不列超过 3 个下一步建议。
