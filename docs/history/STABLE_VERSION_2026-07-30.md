# DailyGlance 稳定记录（2026-07-30）

## 范围

- 新增波段抄底型的展示专用强确认规则：`B8 KDJ 金叉 + B17 超跌止跌反弹`。
- 日线既有首次 `B` 同时包含两项有效计分信号时，显示金色、深色描边的 `B`；右侧状态为“强确认买点”，显示两条实际原因。
- 周线不显示 B/S 标记。其他三套策略不产生 `bQuality` 字段。

## 冻结证据

- 正式样本：90 只股票、8 个指数，统一截止 `2026-07-29`，共 6,183 个可观察 20 日的波段首次 `B`。
- `B8+B17`：77 样本、46 标的；成功率/失败率 `50.67% / 49.33%`，普通 B 为 `33.95% / 63.71%`。
- 5/10/20 日收益为 `2.18% / 1.73% / 6.41%`，延迟一根 K 线并扣除 `0.20%` 成本后仍三个窗口占优。
- 单一标的、指数和年份占比为 `7.79% / 1.30% / 31.17%`，均在准入范围内。
- 冻结规则：`wave-b-quality-20260730-01-b8-b17`；报告为 `.local/strategy-reports/wave-b-quality-report-20260729T175235Z.json`。

## 不变项

- `SIGNAL_VERSION=v4.2.10` 不变。
- 四套策略的 `position`、`simpleAction`、`bsMark`、风险、门禁、离场、冷静期和仓位均不变。
- 冻结前后正式 98 标的基线的四套策略完整报告逐字相同；波段 `B/S=6244/6176`。

## 验证

- JavaScript 语法、完整 `tests/regression.js`、`tests/strategy-cache-fetch.js`、`tests/strategy-iteration.js` 通过。
- 1440px 本地 `status-smoke` 与 `live-dataflow-smoke` 通过；四块 canvas、右侧日期、历史浏览恢复和资源版本 `20260730-01` 正常。
- 外部东方财富历史接口仍偶发 `ERR_EMPTY_RESPONSE`，现有腾讯回退完成确认历史，未造成策略或展示错误。

## 后续

从下一确认交易日开始累计至少 20 个交易日的前向观察。观察期内不放宽 B+ 准入，也不提高仓位。
