# DailyGlance 阶段状态归档（2026-08-09）

> 由 `CURRENT_STATUS.md` 压缩归档；以下内容用于追溯，不再作为当前待办或默认阅读入口。

更新时间：2026-08-09（`v4.2.28` 已发布）

## 当前状态

- 仓位决策已简化为四个正式策略共用的 `0% / 30% / 50% / 80%` 四档；风险系数不再连续缩放仓位，风险、预警、趋势资格和市场门禁只设置最高允许档位。四个策略仍保留各自信号入口和试探仓权限，`B/S` 契约不变。当前线上与本地策略版本均为 `v4.2.28`。
- `v4.2.22` 起已收紧波段抄底型首次 `B` 后第 `1～2` 日的新仓失败离场：冲击下降均线/结构压力后收长上影，且收盘不高于买入日收盘时降一档；收盘跌破买入日最低价时直接归零。仅作用于波段个股日线，`B11` 双层结构防守例外保留。
- `v4.2.23` 已新增波段个股窗口到期防守观察：30%试探仓的积分仅因时间窗口归零时，若买点防守位未破、`MA20` 未下行且 MACD 柱仍为正，保留一日30%观察且不画 `S`；次日需重新站上 `MA5/MA20` 或出现新的有效买入信号，且不得收盘跌破观察日低点，否则离场。原有反弹接管、真实失效、指数和其他策略边界不变；已随 `v4.2.24` 一并发布。
- `v4.2.24` 已收紧波段个股趋势抱单加仓：趋势抱单默认只维持上一交易日仓位；只有已有30%且当日新触发 `B6 缩量回踩不破`、收盘回到 `MA20` 上方、`MA20` 未下行，并通过离场、预警、风险和市场门禁时，才提高到50%。`B6` 不单独触发80%，指数和其他三套策略不变；已发布。
- `v4.2.25` 已补齐波段指数新仓下跌失败：首次 `B` 后两日内，指数盘中跌破买入日低点、收盘虽收回但距防守位不超过0.5%，阴线实体占振幅至少40%、收盘位于振幅底部25%时，提前降一个仓位档，成交量不作硬门槛。上证指数的 `S` 已从 `2026-07-17` 前移到 `2026-07-16`；个股阈值和其他三套策略不变。该版本已发布。
- `v4.2.26` 已新增波段个股 MA20 回踩防守观察：已有30%时，若收盘仅略低于抬升中的MA20、下影明显、无离场/预警/失效且结构位未破，保留30%一日、不加仓、不画 `S`；次日需站回MA20或新触发B6/B11，否则退出。B6原始定义、指数和其他三套策略不变；已发布。
- `v4.2.27` 已新增波段指数新仓冲高失败：首次 `B` 后两日内，指数反抽接近买入日收盘、冲击下降均线或结构压力后留下占振幅至少50%的长上影，阴线收在振幅下方30%且距买入日低点不超过1.5%时，风险仓位降一档；单独长上影不卖。沪深300的 `S` 已从 `2026-07-17` 前移到 `2026-07-16`，个股和其他三套策略不变；定向回归、完整回归、版本同步和桌面 smoke 已通过，并随 `v4.2.28` 发布。
- `v4.2.28` 已接入波段个股到期防守后的B11分层加仓：仅在已有30%、前一日为到期防守观察、当日新触发B11并站上MA5/MA20且MA20不下行时，允许按“30%基础仓＋20%波段仓”提高到50%；核心宽基偏弱时只对此事件放宽普通机会30%新增风险上限。额外20%先减不连带清空仍有效的基础仓，不允许0%直接提高到50%或单独触发80%，指数、其他三套策略和首次 `B/S` 不变。中青旅 `2026-07-27` 为 `30% -> 50%`，`2026-08-07` 为 `50% -> 30%` 且无新 `S`；完整回归、版本同步和桌面 smoke 已通过并发布。
- 四档简化已用同一批 `97` 只股票＋`8` 个指数、共同截止 `2026-07-30` 的 `v4.2.18` 控制组做正式基线对照；四个策略的收益、回撤、换手、持仓天数和 `B/S` 均零漂移，说明本轮主要消除了中间档复杂度，没有改变该快照的结果。报告为 `.local/strategy-reports/formal-strategy-baseline-20260808T022438Z.json`，控制组为 `.local/strategy-reports/formal-strategy-baseline-20260808T023151Z.json`。

- Pages `main=a3e17ea`，资源版本 `20260809-09`、信号版本 `v4.2.28` 已发布；原生 `pages-build-deployment` 运行 `31311089230` 构建与部署成功，GitHub Pages API 状态为 `built`。线上直连仍遇到 GitHub Pages 网络层 `Connection reset by peer`，已按发布护栏停止重试；远端提交、部署状态和文件版本均已确认。Pages 使用 `main / root` 直接分支部署，自定义 `pages.yml` 已移除。
- 本地完整源码在 `codex/full-project`；工作区仍有未提交的离线策略流程收敛和文档改动，不属于 Pages 部署包。
- 发布流程已增加局部补丁快速车道：先比较 `origin/main` 的实际部署差异，纯展示改动只做定向回归和一次桌面 smoke，不再因脏工作区、单纯版本升级或重复验证自动扩大检查范围。
- 生产定位不变：日常观察工具；核心宽基只限制增仓，不制造 `S`；股票显示策略参考仓位，指数显示风险仓位。
- 第一阶段“板块趋势”已接入独立只读扫描层：行业板块全量分页计算多周期趋势，概念板块只取当日涨幅前100做独立热点列表，并为前6个行业板块补充最多6只活跃个股；ETF 信息区分直接对应与“相近参考（非直接对应）”，仍无法确认的行业显示暂无稳定对应关系，不写入核心门禁、个股策略、仓位、`B/S` 或收益计算。
- “板块趋势”页标题下方已恢复极窄的外部环境推测条：复用历史版美股主题证据，把半导体与算力、AI 与云计算、智能电动车、机器人与智能制造的隔夜方向映射为可能受影响的 A 股行业，并固定提示“待 A 股确认”；主时间显示美股交易日而非请求时间，两项主题要求 `2/2` 同向、四项主题至少 `3/4` 同向，主方向使用双倍权重；该数据使用独立缓存，不进入板块趋势分或任何策略计算。
- 板块趋势视觉语义已收口：正式结论色条和整卡染色不再复用于板块概览、概念热点与趋势观察卡，改用中性容器；同一快照的缓存状态只在页头与快照元信息显示，不再逐卡重复。上涨趋势、刚刚转强与单日异动由对应章节标题统一表达，趋势卡内不再重复占一行，状态只保留在卡片无障碍名称中。
- 板块趋势模块已按证据可靠度重排：概览后优先展示上涨趋势、刚刚转强和单日异动，概念热点降至页面末段并标为“今日辅助观察”，避免不纳入趋势统计的热点列表抢占主结果之前的首屏位置。
- 波段抄底型保留未达标时 `30%` 试探；积分达标后撤回永久 `30%` 封顶，改为共用股票趋势 `30% / 50% / 80%` 分层。
- 股票路径已增加趋势高仓资格：中期下降结构最高 `30%`，修复中或趋势数据缺失最高 `50%`，完整多头结构才可使用 `80%`；已有高仓的普通减仓不单独生成 `S`。
- 个股与指数的应用结论已统一为“为什么这么做 / 为什么是当前仓位 / 接下来关注”三行白话说明；覆盖空仓、建仓、加仓、持仓、减仓、离场、冷静期和波段 `B11` 结构防守，历史窗口信号会显示实际发生日期。仓位说明明确区分基础仓位、风险调整和最终固定档位；接下来关注会同时给出恢复条件和防守条件，并按当前积分是否达标选择对应说法。
- 强离场与冷静期的结论和关键依据已统一：此前买入/动能依据失效后，页面有效积分显示为 `0/门槛`；原始窗口积分只保留在折叠技术细节中，避免与离场结论冲突。
- 上述文案优化只翻译既有决策，未改变信号、积分、仓位、市场新增风险限制或主图 `B/S`；四个正式策略继续共用同一套决策边界。
- 三项核心宽基投票、市场门禁只限制增仓的语义保持不变；本阶段不接入股票—行业映射，也不让板块趋势改变生产决策。
- `weak_market_confirmed_80_v1` 已完成97只股票＋8个指数的正式回放；稳健、突破、综合三套策略均为 `reject`，弱市连续确认后 `50% -> 80%` 不接入生产，现有30%/50%门禁保持不变。
- 波段候选 `wave_rejection_protection_v1` 已完成16只 `seed` 股票＋8个指数的快速筛选；回撤改善 `0.6875` 个百分点，但平均收益回退 `1.3462` 个百分点，超出 `0.5` 个百分点保护线，正式状态为 `reject`；不进入全量回放或生产接入。
- 策略验证流程已按目标分为语义修正、`signal_timing` 和核心策略优化三条车道；快筛先选标的再载入，不再先读取105标缓存。候选对照使用本次选中样本的最新共同确认快照和当前生产控制组；信号版本、准入门槛、局部缓存更新或新交易日不再强制重建全量基线。只有核心策略优化或明确的收益主张才默认进入留出/全量。
- 策略工具已增加 `ready_for_product_review` 状态：信号时机候选在硬边界和组合损害预算内，可由用户直接取舍，不再被“回撤必须改善”等错配目标否决。用户已授权继续/上线时，后续在范围不变的前提下不重复请示。
- `wave_rejection_event_guard_v2` 已作为 V1 唯一一次同快照结构性修订完成快筛：全局加仓误伤和指数漂移均为0，受影响日从1731降到160；收益回退收窄到 `0.3808` 个百分点，但回撤反而恶化 `0.0512` 个百分点，状态仍为 `reject`。
- 用户已明确将 V2 按“卖点信号时机调整”批准生产接入：保留上述旧 `risk_control` 报告与 `reject` 记录，但不再用 `drawdown_improvement` 阻断，不重跑105标的研究。生产规则为风险日实时降一档，`30% -> 0%` 当日显示 `S`，事件后只局部阻止该标旧积分回补，普通加仓与指数路径不变。
- 波段首次 `B` 后第1～2个交易日新增新仓压力失败保护：只有冲击下降 `MA20/MA60` 或确认结构高点，同时满足盘中浮盈、爆量、长上影、弱收盘与盈利大幅回吐时才降一档；压力位本身不卖。常山北明 `2026-06-10` 定向复算为 `30% -> 0%`，页面状态为“新仓失败离场”。
- `wave_l10_trend_handoff_warning_v1` 已获用户批准并按冻结定义接入本地生产：波段个股已有持仓且 `收盘价 > MA20 > MA60`、`MA20` 不低于5个交易日前时，单独 `L10` 最多降至30%预警，不归零、不画 `S`，也不启动后续冷静期；`L3/L4/L9`、指数、其他三套策略和既有风险链不变。
- `wave_l10_high_reclaim_reentry_v1` 保留独立候选报告但不接入生产，不与候选一组合。
- 本轮补齐共同确认快照至 `2026-07-30`，随后完成 `wave_independent_trend_recovery_v1` 常山北明定向复算：收益增加 `42.2897` 个百分点，但最大回撤恶化 `2.9612` 个百分点、换手增加 `13.0841%`、有效影响日 `22`，仍为 `reject`，不进入快筛。
- 在同一新快照上独立验证 `wave_failed_short_trade_reentry_lock_v1`：16 只 seed 股票＋8 个指数快筛通过继续全量门槛；105 标全量回放后收益回退 `0.0260` 个百分点、最大回撤改善 `0.5873` 个百分点、换手下降 `5.0702%`，但效率目标要求换手至少下降 `10%`，且时间窗口 `0/3`，正式状态为 `reject`，不接入生产。
- `wave_expiry_b11_takeover_add_v2` 的影子报告与原始测量结果保持不变：97只股票＋8个指数全量只命中中青旅 `2026-07-27` 与金山办公 `2021-10-15` 两次，指数与主图 `B/S` 零漂移；平均收益变化 `-0.0069` 个百分点、平均最大回撤变化 `+0.0062` 个百分点、换手增幅 `0.0112%`。用户已批准按冻结定义接入并发布，生产版本为 `v4.2.28`。

## 风险与待验证

- 中青旅 TickFlow 确认历史已定向复算：`2026-08-07` 收盘 `7.03`，仅略低于抬升中的 `MA20=7.04`，下影明显，且无离场、预警、失效或结构破位；该日原始信号为空，不满足 B6。结果由原 `30% -> 0% + S` 调整为“MA20回踩观察”，保留30%且不画 `S`；次日必须站回MA20或新触发B6/B11接管。已覆盖无明显下影、偏离过大、MA20下行、观察到期退出和指数零影响；策略分组、完整回归、版本同步检查和桌面状态 smoke 均已通过，并随 `v4.2.26` 发布。
- 中青旅真实确认历史已定向复算：`2026-07-24` 从原来的 `30% -> 0% + S` 改为“到期防守观察”并保留30%；`2026-07-27` 重新站上 `MA5/MA20` 且出现新的回踩信号，30%被正常接管。已覆盖次日未恢复则离场、新信号接管、`MA20` 下行不宽限、真实失效不宽限和指数零影响；完整回归、版本同步和桌面状态 smoke 已通过。
- 天府文旅真实缓存已定向复算：`2026-06-15` 保持 `30% + B`，`2026-06-16` 改为 `0% + S`，右侧状态为“新仓失败离场”。已覆盖收盘仍高于买入价、未触压力区、指数路径和 `B11` 结构例外；完整回归、版本同步和桌面状态 smoke 已通过。
- 伊力特 TickFlow 确认历史已定向复算：`2026-08-07` 保持 `30% -> 50%` 且不画新的 `B/S`，现在明确由当日 `B6` 触发“趋势修复加仓”；前一日已在 `MA20` 上方，因此右侧不再误写“重新站回20日线”。已覆盖无当日 B6、历史 B6、`MA20` 下行、风险上限、指数和其他策略边界；策略分组、完整回归、版本同步检查和桌面状态 smoke 均已通过。
- 上证指数确认历史已定向复算：`2026-07-14` 首次 `B`，`2026-07-15` 降至30%，`2026-07-16` 盘中跌破买入日低点后弱收阴线，现提前 `30% -> 0% + S`；原 `2026-07-17` 的 `S` 已取消，`2026-07-20` 新信号重新生成 `B`。已覆盖普通下影不卖、个股原阈值不变和其他策略零影响；策略分组、固定快照、完整回归、版本同步检查和桌面状态 smoke 均已通过。
- 行情源均为公开接口，无 SLA，仍存在外部网络波动风险。
- 四档仓位定向回归、完整回归、版本同步、`check.js --full` 和桌面状态 smoke 均已通过；资源版本、实时覆盖状态、右侧结论与 K 线/成交量/MACD/KDJ 四张图均正常。`check.js --full` 仅保留既有“归档脚本目录不存在”警告，无失败；Pages 原生构建与部署成功。
- 新仓压力失败候选在常山北明定向样本捕获2个历史事件，作用域零违规并达到 `ready_for_product_review`；16只 seed 股票截至 `2026-07-29` 没有满足全部冻结条件的事件，因此保持严格阈值、不为扩大样本放宽条件。生产定向回归、完整回归、版本同步和桌面 smoke 均已通过。
- `wave_failed_trial_reentry_v1` 已完成常山北明定向复算和16只 seed 股票＋8个指数快筛：定向在 `2024-07-22` 新增一次30%重新建仓，但随后仍在8月初退出，收益相对生产回退 `1.6249` 个百分点；快筛影响决策日为0、状态为 `insufficient_evidence`。候选已冻结，未修改生产信号、仓位、版本或 `B/S`。
- `wave_same_day_repair_confirmation_entry_v2` 定向复算能在 `2024-08-22` 新增30%买入，但同时在其他时段反复触发，常山北明样本新增7组 `B/S`、换手增幅 `14.511%`，超过 `10%` 保护线，状态为 `reject`。候选已冻结，未接入生产。
- 旧 `v4.2.18` 控制组曾覆盖常山北明该段主升：`2024-09-11` 以20%建仓，`2024-09-26` 不误卖，`2024-10-08` 恢复到30%；`2024-10-25` 单独 `L10` 保持30%且不画 `S`，`2024-10-28` 按既有风险链降至10%，直到 `2024-11-07` 出现 `L3` 才清仓并画 `S`。其中20%/10%属于旧中间档，当前四档版不再生成。
- 策略流程调整已通过评估器、持续迭代入口和正式候选报告契约测试；另用 1 只股票＋8 个指数上下文完成最小端到端检查，报告确认实际只载入 9 个标的，使用 `v4.2.15` 当前生产控制组，未重建或运行105标全量。
- 提交 `9b0ee18` 只删除重复的自定义 Pages 工作流，GitHub 自带 `pages-build-deployment` 已成功；本次没有再触发 `Deploy DailyGlance`，Pages API 确认 `legacy + main / root` 且状态为 `built`。线上直连此前两次遇到 `Connection reset by peer`，按发布快速车道不再循环重试。
- 弱市80%候选的全量回放显示收益普遍回退、回撤和换手增加；稳健/综合在后续样本、科技或周期分层越线，突破追涨的成本压力检查失败。完整证据见 `docs/history/strategy/STRATEGY_VALIDATION_LOG_2026-08.md`。
- 波段冲高回落组合候选在快筛中只触发23次风险日，但“所有加仓都要新信号”和再入锁共扩大到上千个决策日，是本轮收益回退的主要结构性风险。
- V2 作为旧 `risk_control` 候选时没有达到“回撤必须改善”的门槛；该门槛已确认不适用于本次卖点时机修正。实际收益/回撤变化已由用户接受，当前风险转为生产实现和展示必须严格等同于 V2 冻结定义。
- 波段 L10 趋势接管生产定向复算、策略决策分组回归、完整回归和桌面状态 smoke 均已通过；边界覆盖单独 `L10`、`L3+L10`、指数零例外和接管后不进入冷静期。Pages 原生 `pages-build-deployment` 已成功；线上直连仍遇到已知的 `Connection reset by peer`，按发布快速车道不再循环重试。
- 新快照研究报告已保留：趋势恢复定向报告为 `.local/strategy-reports/formal-strategy-candidate-lab-20260807T172925Z.json`，失败短交易锁定快筛为 `.local/strategy-reports/formal-strategy-candidate-lab-20260807T173052Z.json`，全量报告为 `.local/strategy-reports/formal-strategy-candidate-lab-20260807T173728Z.json`。两项均未修改生产信号、仓位、版本或 `B/S`。
- 到期防守观察后的B11加仓候选全量样本仅有2次事件：中青旅随后1/3/5日为 `+0.72% / +2.30% / +4.75%`，金山办公为 `-3.78% / +3.47% / -0.23%`；组合损害预算与硬边界均通过，但事件数仍少，不构成收益优势证明。报告为 `.local/strategy-reports/wave-expiry-b11-takeover-add-v2-screen-20260809T103126855Z.json`、`.local/strategy-reports/wave-expiry-b11-takeover-add-v2-full-20260809T104351692Z.json` 和 `.local/strategy-reports/wave-expiry-b11-takeover-add-v2-cross-strategy-20260809T104718500Z.json`。

## 下一步

- 如需继续第二阶段行业板块门禁，先补齐稳定的股票—行业指数映射和历史数据契约，再作为独立候选验证，不直接混入三项核心宽基投票。
- 不在当前快照上继续为弱市80%候选叠加天数、均线或手工防御标签；只有新样本或稳定的行业相对强弱数据就绪后才重启。
- `wave_rejection_protection_v1` 继续冻结，不修改定义或复活原候选；也不自动开始积分到期续持候选。
- V2 生产代码、回归、桌面 smoke、版本同步和 Pages 发布已完成；不创建 V3，不重跑候选快筛或全量研究。
- 新仓压力失败保护已按 `origin/main` 实际 Pages 差异发布；本地策略研究、测试和文档未进入部署包。
- 不把“硬失效后5日内重新上车”接入生产，也不在当前快照继续延长观察期或持有保护；若有新的独立案例，再另行研究更严格的同日修复＋右侧确认共振。
- 同日修复＋右侧确认 V2 已在定向样本上 `reject`，不运行快筛、全量回放或 V3；保留当前9月重新建仓和十字星误卖修复。
- `wave_independent_trend_recovery_v1` 已完成其允许的唯一同快照结构性修订；即使新快照仍被拒，也不继续增加锁存、价格、天数或风险标签过滤。`wave_failed_short_trade_reentry_lock_v1` 全量也已拒绝，不延长锁定期或另造同类 V2；后续只有更长的新确认样本，或独立的行业相对强弱数据就绪后，才注册新的研究问题。
- `wave_expiry_b11_takeover_add_v2` 已按基础层与波段层分离接入生产并发布；不全局取消核心宽基偏弱下普通机会30%新增风险上限，也不在当前样本继续扩大该例外。
- 线上直连仍可能遇到已知的 `Connection reset by peer`；本次 Actions 成功且远端部署文件已确认资源版本 `20260809-09`，不循环重试，也无需重复发布。
