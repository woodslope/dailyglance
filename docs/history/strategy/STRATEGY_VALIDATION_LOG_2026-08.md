# 策略验证记录（2026-08）

本文记录“核心宽基偏弱”命名、硬上限优先和30%/50%分层增量门禁的验证证据，以及2026-08-02对验证复杂度的纠偏。最终将这两项定位为决策语义修正，不再由成本压力或指数分层结果决定是否发布。

## 统一四档仓位（2026-08-08）

- 本轮目标是降低仓位决策和回测复杂度，不改变四个策略的信号入口、冷静期、风险事件或 `B/S` 契约。
- 生产仓位统一为 `0% / 30% / 50% / 80%`；风险、预警、趋势资格和市场门禁只设置最高允许档位，不再通过连续风险系数生成 `10%`、`20%`、`40%`、`60%` 等中间档。
- 四个策略仍保留各自入口：稳健趋势和突破追涨未达标时不试探，波段抄底和综合全能可用 `30%` 试探；正式达标统一进入 `80%` 基础档，趋势抱单统一为 `50%`。
- 使用同一确认快照（97只股票＋8个指数，共105个标的，共同截止日 `2026-07-30`）对比本地 `v4.2.19` 与 `origin/main` 的 `v4.2.18` 控制组。四个策略的收益、最大回撤、换手、持仓天数和 `B/S` 均零漂移；报告分别为 `.local/strategy-reports/formal-strategy-baseline-20260808T022438Z.json` 与 `.local/strategy-reports/formal-strategy-baseline-20260808T023151Z.json`。
- 结论：本轮先完成低风险的决策表示收敛，未在该快照引入收益或回撤主张；后续若要继续调整风险上限或买卖时机，另行注册策略候选。

## 候选定义

- `hard-cap-priority`：强离场、冷静期、风险评分、预警和 `W4` 等硬上限优先于仓位回差；仓位回差只抑制向上的普通10%波动。
- `weak-market-tiered-30-50`：核心宽基偏弱时，普通机会新增风险上限30%，标的自身独立走强上限50%；门禁不主动压缩已有高仓位，不制造 `S`。
- 两项都属于已有决策契约的语义修正，不增加行业/板块数据、外部行情API或离线主题映射。
- 候选定义哈希分别为 `f09b2953ed2107bab37254f28adec73371509d7aef23d09472a0130e74fd2668` 和 `e0384996073eefac3a5a63754258b1dddae0dcc716cf3d77b6f5dea71c6803e1`。
- 本轮回放时的对照策略版本为 `v4.2.12`；正式接入后提升为 `v4.2.13`。

## 快速筛选

运行日期：2026-08-01。

范围为8个指数和16只 `seed` 股票，共24个标的；四策略均参与，因此聚合统计单位为“策略 x 标的”。共同确认截止日为 `2026-07-29`，快照哈希为 `814b107b6cb36feb46b1715f26dbb57b11512caf24e464f99458c7749848d5f9`。

| 候选 | 状态 | 平均收益变化 | 平均最大回撤变化 | 受影响决策日 | B/S变化 |
| --- | --- | ---: | ---: | ---: | ---: |
| 硬上限优先 | `continue_full` | `+0.0761` 个百分点 | `-0.0867` 个百分点 | 894 | `+1 / +1` |
| 30%/50%分层门禁 | `continue_full` | `+0.5663` 个百分点 | `-1.1943` 个百分点 | 5226 | `0 / 0` |

两项候选均满足快速筛选的继续正式回放条件。快速筛选只决定是否继续，不构成生产准入。

## 105标的正式回放

正式范围为97只股票和8个指数，共105个标的、四个策略；聚合摘要中的420个统计行是 `105 x 4`，不是420个不同标的。共同确认截止日为 `2026-07-29`，候选报告快照哈希为 `1d1d8bc9116b1835096ff3862647c4a7927bbd4b3b1bc00085a6b276dbaf693c`。回放覆盖三段126日窗口、标准/0.20%/0.30%成本、延迟一根K线，以及股票、指数、压力、高波动和科技分层。

| 候选 | 聚合状态 | 平均收益变化 | 平均最大回撤变化 | 受影响决策日 | 完整交易 | B/S变化 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 硬上限优先 | `recommend_shadow` | `-0.1028` 个百分点 | `-0.1617` 个百分点 | 7181 | 18091 | `+6 / +6` |
| 30%/50%分层门禁 | `recommend_shadow` | `+1.3029` 个百分点 | `-1.9221` 个百分点 | 25770 | 18085 | `0 / 0` |

聚合层的收益、回撤、三窗口、样本量、股票、指数、压力、高波动、科技和成本压力检查均通过。30%/50%门禁没有改变第一次 `B/S`；硬上限候选的6组 B/S 变化全部来自波段抄底型硬风控恢复正确的退出/再入路径，不是宽基门禁制造卖点。

分策略结果不能被聚合状态隐藏：

- 波段抄底型在硬上限候选的成本压力差值为 `-0.3842` 个百分点，低于 `-0.25` 个百分点门槛，单策略状态为 `reject`。
- 波段抄底型在30%/50%候选的指数分层收益差值为 `-1.8402` 个百分点，低于 `-1.00` 个百分点门槛，单策略状态为 `reject`。
- 稳健趋势型、突破追涨型和综合全能型在两项候选下均为 `recommend_shadow`。

上述分项结果保留为研究观察，但不再作为这次语义修正的发布门槛。成本压力和指数分层用于评估新信号、新积分、交易频率变化或收益宣传；本轮不改信号定义或积分阈值，关键判断是宽基不得覆盖已走强标的、不得强减已有仓位，以及硬风控必须优先。

## 最终处理

- 删除前端策略健康清单和“观察级/验证级”展示，避免把离线研究状态带入日常使用。
- 删除一次性门禁实验脚本；验证数值留在本历史记录中，不再增加维护面。
- 按30%/50%门禁、硬风控优先、已有高仓不被宽基强减、数据缺失禁止新增风险和 `B/S` 契约做定向回归。
- 生产接入版本为 `APP_BUILD=2026-08-02-01`、`SIGNAL_VERSION=v4.2.13`。
- 以后只有新增/删除信号、改积分阈值、改首次 `B/S` 或交易频率、或需要评价收益优势时，才将成本压力与分层结果作为正式准入证据。

## 弱市连续确认80%候选（2026-08-06）

### 候选边界

- 候选 ID：`weak_market_confirmed_80_v1`，定义哈希 `efc5c7a4f5d7948dfe09cc8f7294e7834344795e034e154bdf456a51094e4693`。
- 只允许稳健趋势型、突破追涨型和综合全能型的股票路径，在核心宽基连续偏弱、个股前后两个交易日均满足 `independent`、前一日最终仓位为 `50%`，且生产目标仓位为 `80%` 但被弱市门禁压至 `50%` 时，将候选仓位放宽为 `80%`。
- 不改首次建仓、减仓、清仓、指数路径、波段抄底型或首次 `B/S`。
- 对每次放宽事件逐条检查股票范围、前后日市场/个股资格、`50% -> 80%`转移、原门禁 `50%`、候选门禁 `80%` 与 `B/S` 一致性；快速筛选和正式回放均为零违例。

### 快速筛选

范围为16只 `seed` 股票和8个指数，共同确认截止日 `2026-07-29`，数据哈希 `814b107b6cb36feb46b1715f26dbb57b11512caf24e464f99458c7749848d5f9`。三套策略均为 `continue_full`，`B/S` 零漂移。

| 策略 | 放宽命中 | 受影响日 | 收益变化 | 平均最大回撤变化 |
| --- | ---: | ---: | ---: | ---: |
| 稳健趋势型 | 109 | 421 | `-0.6016` 个百分点 | `+0.4877` 个百分点 |
| 突破追涨型 | 29 | 119 | `-0.3563` 个百分点 | `+0.4190` 个百分点 |
| 综合全能型 | 105 | 381 | `-0.3621` 个百分点 | `+0.5160` 个百分点 |

### 105标的正式回放

范围为97只股票和8个指数，对照为 `SIGNAL_VERSION=v4.2.14`，回放覆盖三段126日时间窗口、标准/成本压力、延迟成交以及股票、后续样本、高波动、科技、防御和周期分层。

| 策略 | 正式状态 | 受影响日 | 收益变化 | 平均最大回撤变化 | 换手变化 | `B/S` |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 稳健趋势型 | `reject` | 1770 | `-0.9472` 个百分点 | `+0.9069` 个百分点 | `+4.64%` | `0 / 0` |
| 突破追涨型 | `reject` | 436 | `-0.3126` 个百分点 | `+0.3702` 个百分点 | `+5.11%` | `0 / 0` |
| 综合全能型 | `reject` | 1617 | `-0.6928` 个百分点 | `+0.7149` 个百分点 | `+3.82%` | `0 / 0` |

拒绝原因：

- 稳健趋势型：股票层收益 `-1.0253` 个百分点，后续样本收益/回撤 `-1.8184 / +1.3290` 个百分点，科技层收益 `-1.7206` 个百分点，防御层回撤 `+1.3787` 个百分点，周期层收益/回撤 `-1.4996 / +1.1624` 个百分点，成本压力检查失败。
- 突破追涨型：总体和各主要分层在容差内，但成本压力差值 `-0.3734` 个百分点，低于 `-0.25` 个百分点门槛。
- 综合全能型：后续样本收益/回撤 `-1.6756 / +1.0636` 个百分点，科技层收益 `-1.7200` 个百分点，周期层收益/回撤 `-1.3477 / +1.2164` 个百分点，成本压力检查失败。

持久化报告：

- 稳健趋势型：`.local/strategy-reports/formal-strategy-candidate-lab-20260805T204916Z.json`
- 突破追涨型：`.local/strategy-reports/formal-strategy-candidate-lab-20260805T204925Z.json`
- 综合全能型与稳健趋势型并行完成时生成了相同的秒级文件名，独立 JSON 被覆盖；完整进程输出和上表聚合结论已保留。该问题不改变 `reject` 结论，候选若重启，必须先修正并行报告命名或改为串行输出。

### 结论与重启条件

- 三套策略均为正式 `reject`，候选不接入生产、不进入影子观察，不修改个股决策、仓位、市场门禁、版本或首次 `B/S`。
- 当前30%/50%弱市分层门禁保持不变；“弱市连续确认后50% -> 80%”不是可用的全局仓位规则。
- 只有新增独立交易时段/新标的，或建立稳定的股票—行业强弱数据并能明确区分“防御性逆势”与“弱市追高”时，才能以新候选重启；不在同一快照上继续叠加均线、天数或阈值过滤。

## 波段冲高回落保护候选（2026-08-07）

### 候选边界

- 候选 ID：`wave_rejection_protection_v1`，定义哈希 `05db441e487c8713abf120b07f1b7488653aec0fcf4fbc683907e6126f80ac6f`。
- 仅影响波段拄底型个股路径：持仓浮盈至少 `8%`、成交量至少为前5日均量 `1.5` 倍、上影线至少为实体 `1.5` 倍、收盘位于全日振幅下半区，且高点接近最近20日压力时，候选仓位降一档。
- 候选同时冻结退出日之前的旧积分，至少等待2个交易日且退出后新信号重新达标才解锁；无最近2日新有效信号或存在 `W2/W3` 时禁止加仓。
- 对照基线报告为 `.local/strategy-reports/formal-strategy-baseline-20260807T031939Z.json`；候选快筛报告为 `.local/strategy-reports/formal-strategy-candidate-lab-20260807T032619Z.json`。

### 快速筛选

范围为16只 `seed` 股票和8个指数，共同确认截止日 `2026-07-29`，数据哈希 `814b107b6cb36feb46b1715f26dbb57b11512caf24e464f99458c7749848d5f9`。候选仅影响股票，指数仓位、动作和 `B/S` 零漂移。

| 状态 | 触发事件 | 受影响决策日 | 平均收益变化 | 平均最大回撤变化 | 换手变化 | `B/S` 变化 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `reject` | 23 | 1731 | `-1.3462` 个百分点 | `-0.6875` 个百分点 | `-12.96%` | `-44 / -44` |

准入检查中，平均最大回撤改善通过（要求至少改善 `0.5` 个百分点，实际改善 `0.6875`），但平均收益保护失败（允许最多回退 `0.5` 个百分点，实际回退 `1.3462`）。

诊断显示，放量冲高回落本身只触发23次（其中22次从试探仓退出），但“无新信号不加仓”阻止737次、再入锁阻止309次、`W2/W3` 阻止45次。候选把止盈、再入和所有加仓确认绑在一起，实际影响面远大于23个风险日，是收益回退的主要结构性风险。

### 目标案例核对

常山北明 `2026-08-04` 尚未进入本轮截止日为 `2026-07-29` 的正式研究快照，因此只用同源收盘确认数据做定向复算，不冒充正式快筛证据。该日相对 `2026-07-21` 建仓收盘价的浮盈为 `13.14%`，量比为 `1.92`，上影/实体为 `2.30`，收盘位置为全日振幅的 `39.22%`，并接近20日压力；按候选定义会触发 `30% -> 0%`。次日最高价 `14.33` 低于风险日高点 `14.57`，且尚在再入锁内，不会从旧积分自动加仓。

### 结论与重启条件

- 快速筛选正式状态为 `reject`；不进入全量回放、影子观察或生产接入，不修改波段生产信号、积分、仓位、版本或 `B/S`。
- 当前组合候选不能在同一快照上继续调参。若新增独立交易时段/新标的，或再出现可定位的真实错误案例，只能把“冲高回落分档止盈”与“所有加仓要求新信号”拆成独立原子候选重启，不再用一条规则扩大到全部持仓天。

## 波段冲高回落事件局部修订候选（2026-08-07）

### 候选边界

- 候选 ID：`wave_rejection_event_guard_v2`，定义哈希 `a6988fb38998d82c89a868f0cf3b2b3a1c507e01dc1d9bf94dcf92f5bcef4be2`，`revisionOf=wave_rejection_protection_v1`。
- 这是 V1 在同快照上唯一一次结构性修订；浮盈、量比、上影/实体、收盘位置、压力回看和最短锁定天数均保持 V1 数值不变。
- 删除“所有加仓都要新信号”和“必须重新积满4分才解锁”；只在真实冲高回落事件后局部锁定，以收复风险日高点、事件后新信号，或至少两日后站回风险日收盘且无 `W2/W3` 解锁。首次回补最多 `30%`，再保持一个交易日低仓。
- 硬边界为：风险事件前零漂移，全局无新信号/`W2/W3` 加仓阻止必须为0，指数零漂移。

### 快速筛选

- 报告：`.local/strategy-reports/formal-strategy-candidate-lab-20260807T040325Z.json`。
- 范围为16只 `seed` 股票＋8个指数，共同确认截止日 `2026-07-29`；结果只作同快照探索性快筛。
- 触发26次，其19次清仓、7次降一档；受影响决策日160天、13只股票。局部再入锁阻欤24天，恢复仓位封顶5次。
- 事件发生前变化0天，全局无新信号加仓阻止0天，全局 `W2/W3` 加仓阻止0天，指数漂移0天，说明 V1 的作用域错误已被消除。

| 状态 | 受影响决策日 | 平均收益变化 | 平均最大回撤变化 | 换手变化 | `B/S` 变化 |
| --- | ---: | ---: | ---: | ---: | ---: |
| `reject` | 160 | `-0.3808` 个百分点 | `+0.0512` 个百分点 | `+0.0809%` | `+10 / +10` |

收益回退已回到 `0.5` 个百分点保护线内，但风险控制候选必须至少改善 `0.5` 个百分点回撤；实际反而恶化 `0.0512` 个百分点，因此 `drawdown_improvement` 失败。

V1 的回撤改善主要来自过度压低整体暴露，而不是“单日冲高回落就卖出”本身稳定有效；作用域纠正后，这条硬卖出规则没有保住回撤改善。

### 目标案例复核

- TickFlow 同源收盘数据再次确认：常山北明 `2026-08-04` 收盘 `13.95`、高点 `14.57`，相对 `2026-07-21` 收盘 `12.33` 浮盈 `13.14%`，量比约 `1.92`，上影/实体 `2.30`，收盘位置 `39.22%`。
- 当日 KDJ 仍为 `K>D`（`77.01 > 66.41`），但并非当日新金叉；前一交易日已是 `77.40 > 61.11`。当日原始信号为 `B4` 放量突破，但 `B4` 不进入波段抄底型有效积分。
- `2026-08-05` 高点 `14.33` 仍未收复风险日高点，原始信号为只观察的 `W2`。V2 会在8月4日 `30% -> 0%`、8月5日保持0%，但 V2 已被否决，该动作不进入生产。

### 结论与重启条件

- V2 正式状态为 `reject`；按流程立即停止，不运行非 `seed` 留出样本、全量回放、影子观察或生产接入，不在同快照创建 V3。
- 当前证据不支持“单日冲高回落直接卖出”成为波段通用规则。若新时段或新样本到位，可以全新候选研究“风险日只标记，后续跌破风险日收盘/低点再减仓，收复高点则取消”的二次确认模型；这是新研究方向，不在本快照继续试。
- 后续重启时将其归入“信号时机适配”车道，重点先看事件级误卖/漏卖和后续走势，而不把“必须改善 `0.5` 个百分点回撤”当作单次快筛的唯一门槛；这不改变 V2 已被冻结的结论。

### 人工产品决策与生产接入（2026-08-07）

- 用户明确将本问题定义为“卖点信号时机调整”，不要求候选必须带来回撤改善，并明确接受平均收益 `-0.3808` 个百分点、平均最大回撤 `+0.0512` 个百分点的观察结果。
- 原 `risk_control` 报告和 `reject` 状态保留，不改写历史证据；但其唯一失败项 `drawdown_improvement` 不再作为本次卖点时机修正的产品接入阻断。
- 用户批准按 V2 已冻结定义直接接入，不改数值参数，不新建 V3，不重跑105标的研究：风险日实时降一档；`30% -> 0%` 当日显示 `S`；事件后只在该标的局部阻止旧积分立即回补；无事件的加仓和指数路径不变。
- 二次确认版未获批准，不进入生产。

## 波段首次建仓压力失败保护（2026-08-07）

- 候选 ID：`wave_fresh_entry_pressure_failure_v1`，类别为 `signal_timing`。只覆盖波段抄底型个股首次 `B` 后第 `1～2` 个交易日；压力位本身不卖，必须同时满足盘中浮盈至少 `5%`、收盘浮盈不超过 `2%`、盈利回吐至少 `60%`、前5日均量至少 `2` 倍、上影至少占振幅 `35%`、收盘位于振幅下方 `35%`，并冲击下降中的 `MA20/MA60` 或最近确认结构高点。
- 常山北明定向报告 `.local/strategy-reports/formal-strategy-candidate-lab-20260807T055447Z.json` 捕获 `2026-06-10`：`30% -> 0%`，盘中浮盈 `6.08%`、收盘浮盈 `0.98%`、回吐 `83.87%`、量比 `3.16`、上影占振幅 `74.29%`，冲击下降中的 `MA20` 后收回。该标的另有一个历史同型事件；作用域检查无违规，组合观察收益增加 `6.3874` 个百分点、最大回撤改善 `2.7330` 个百分点，状态为 `ready_for_product_review`。
- 16只 seed 股票与8个指数截至 `2026-07-29` 未出现满足全部冻结条件的事件，因此结果为 `insufficient_evidence`；不为扩大样本放宽阈值。用户已按建议批准，按漏卖时机修正接入生产；首次 `B`、买入积分、指数和其他三套策略不变，事件后的局部恢复沿用既有冲高回落保护。

## 波段试探仓硬失效后的重新确认候选（2026-08-07）

- 候选 ID：`wave_failed_trial_reentry_v1`，类别为 `signal_timing`。只覆盖波段抄底型个股：生产 `20%/30%` 试探仓因买入信号 `price-break` 硬失效清仓后，进入最多5个交易日观察；必须先出现清仓后的新修复信号，再由 `B3/B15`、收盘站上 `MA20`、收复原试探与清仓高点、未再创新低、无 `L/W`、风险系数为1且市场允许增加风险共同确认，才允许一次 `0% -> 30%` 重新建仓。重新建仓后最多2个交易日只阻止旧积分立即清仓，风险系数仍可把试探仓降至20%，任何风险信号、冷静期或跌破重新建仓低点都不保护。
- 常山北明定向报告 `.local/strategy-reports/formal-strategy-candidate-lab-20260807T101324Z.json` 命中 `2024-07-22` 重新建仓；但该次确认并非最终主升前的有效上车点，候选随后仍在8月初退出。相对生产控制组，累计收益变化 `-1.6249` 个百分点、换手增加 `1.8927%`、新增 `B/S` 各1次，未解决目标案例并越过信号时机收益保护线。
- 快筛报告 `.local/strategy-reports/formal-strategy-candidate-lab-20260807T101538Z.json` 覆盖16只 `seed` 股票和8个指数，共同截止 `2026-07-29`，数据哈希 `bd0e9ce191a9f78df645ecc47c1c9ab261c53b4e8263260a3ea3dac9f15f1938`。候选影响决策日为0，状态为 `insufficient_evidence`；指数零漂移，但没有跨标的事件证据。
- 结论：候选冻结，不接入生产、不改 `SIGNAL_VERSION`、仓位或主图 `B/S`。现有证据说明“硬失效后很快重新上车”容易在真正主升前再次承受一段无效反弹，不能作为通用修复。若后续重启，必须使用新的独立样本研究“修复信号与右侧确认同日共振”或更晚的结构突破，不在本候选上延长等待期、放宽风险或继续增加持有天数。

## 波段同日修复＋右侧确认候选（2026-08-07）

- 候选 ID：`wave_same_day_repair_confirmation_entry_v2`，是已否决 `wave_right_confirmation_entry_v1` 在同一数据快照上的唯一结构性修订。只允许波段抄底型个股空仓日，当日同时出现修复信号与 `B3/B15`，收盘站上 `MA20`、无 `L/W`、风险系数为1且市场门禁允许时，建立最多30%试探仓；不恢复旧积分，后续完全沿用生产风险和离场链。
- 常山北明定向报告 `.local/strategy-reports/formal-strategy-candidate-lab-20260807T105847Z.json` 中，候选在 `2024-07-25`、`2024-08-16`和 `2024-08-22` 均会新增30%买入。其中7月25日的仓位于8月5日退出；8月16日的仓位在8月19日触发新仓失败离场；8月22日虽再次买入，但受上一次风险事件的分步恢复链影响，仓位于8月23日降至20%、8月26日降至10%、8月28日归零，仍未通过“更早重新上车”稳定覆盖主升段。
- 定向样本共确认8次同日入场事件，作用域诊断中指数漂移和无归因新增 `B` 均为0；但候选新增7组 `B/S`，换手增幅 `14.511%`，超过 `10%` 保护线，成本压力下收益变化为 `-0.4697` 个百分点，正式状态为 `reject`。
- 当前生产控制组已能覆盖这段主升：`2024-09-11` 以20%重新建仓；`2024-09-26` 因上影不长于下影，不再触发“冲高回落”错误 `S`，仅按常规风险降至10%；`2024-10-08` 恢复至30%，`2024-10-25` 出现 `L10` 后清仓。
- 结论：V2 冻结，不接入生产，不运行快筛、全量或第三套参数修订。本轮保留已生效的十字星/上下影误判修复和9月右侧重新建仓链，不为提前十几个交易日上车引入反复交易。

## 波段候选新确认快照复算（2026-08-08）

### 快照与边界

- 本轮把本地确认历史刷新到共同截止日 `2026-07-30`；随后指数和 seed 股票继续刷新到 `2026-08-07`，正式候选按全体可用标的对齐到 `2026-07-30`，未使用盘中实时数据。
- 生产控制组、`SIGNAL_VERSION`、仓位规则和主图 `B/S` 均未修改；报告只写入 `.local/strategy-reports/`。

### 趋势恢复候选新快照定向复算

- 候选 ID：`wave_independent_trend_recovery_v1`，仍是 `wave_trend_position_recovery_v1` 的唯一同快照结构性修订，不新增第四版参数。
- 常山北明报告：`.local/strategy-reports/formal-strategy-candidate-lab-20260807T172925Z.json`；共同截止日 `2026-07-30`。
- 收益变化 `+42.2897` 个百分点，但平均最大回撤恶化 `+2.9612` 个百分点、换手增加 `+13.0841%`，受影响决策日 `22`，正式状态 `reject`。
- 结论：趋势恢复确有收益价值，但当前“20日新高＋完整多头”恢复到50%的实现仍会造成重复仓位接管和回撤/交易成本越界；不进入快筛，不再增加锁存、天数、价格或风险标签过滤。

### 失败短交易旧证据锁定候选全量复算

- 候选 ID：`wave_failed_short_trade_reentry_lock_v1`，类别 `efficiency`；先通过16只 seed 股票＋8个指数快筛，再完成105标全量回放。
- 快筛报告：`.local/strategy-reports/formal-strategy-candidate-lab-20260807T173052Z.json`；全量报告：`.local/strategy-reports/formal-strategy-candidate-lab-20260807T173728Z.json`；共同截止日 `2026-07-30`。
- 全量结果：收益变化 `-0.0260` 个百分点，平均最大回撤改善 `0.5873` 个百分点，换手下降 `5.0702%`，完成交易减少 `329` 笔，`B/S` 各减少 `329` 次；指数路径零漂移。
- 正式状态 `reject`：换手降幅未达到效率候选至少 `10%` 的门槛，三段时间窗口仅 `0/3` 通过；收益、回撤、成本压力和股票稳定性均未越界。
- 结论：锁定旧证据方向能减少往返，但当前5日锁定不足以稳定达到目标，不能接入生产；不延长锁定期，不在同一研究问题上创建 V2。

### 收口

- 本轮两个候选均冻结；生产策略、版本、仓位、信号和 `B/S` 保持不变。
- 后续只有更长的新确认交易时段，或稳定的股票—行业相对强弱数据就绪后，才注册新的研究问题；不在当前快照继续微调趋势恢复或失败重入参数。

## 波段指数新仓冲高失败时机修正（2026-08-09）

- 候选 ID：`wave_index_fresh_entry_rejection_v1`，类别 `signal_timing`，定义哈希 `0754d7bcffebc8743717f902a2a004e0686534f2f3312a5ec10073f41a0d9f8b`。仅覆盖波段抄底型指数日线首次 `B` 后第 `1～2` 个交易日；个股和其他三套策略不变。
- 冻结条件：盘中高点回到买入日收盘价下方 `1%` 以内、收盘不高于买入日收盘且距买入日低点不超过 `1.5%`，同时收阴、上影占振幅至少 `50%`、收盘位于振幅下方 `30%`，并冲击下降中的 `MA20/MA60` 或最近确认结构高点压力区。指数成交量不作硬门槛，单独长上影不卖。
- 8 个指数确认历史数据哈希 `24475904439678f5e6411cdce13f1552924f30e5a59975d37126b4e7cbc9c213`；事件级对照触发16次、影响35个决策日。平均收益变化 `-0.3842` 个百分点、平均最大回撤变化 `+0.0709` 个百分点、换手增幅 `1.1947%`，均在信号时机车道预算内；稳健、突破和综合策略决策零漂移。报告为 `.local/strategy-reports/wave-index-fresh-entry-rejection-v1-20260809.json`。
- 目标案例沪深300 `2026-07-16` 从 `30%` 降至 `0%` 并生成 `S`，原 `2026-07-17` 的 `S` 取消；风险日后1日指数下跌 `3.60%`。用户已接受组合影响并批准生产接入，结果由 `ready_for_product_review` 转为 `approved_for_production`，不再修改冻结阈值。

## 波段到期防守观察后的 B11 分层加仓候选（2026-08-09）

### 候选边界

- 候选 ID：`wave_expiry_b11_takeover_add_v2`，类别 `signal_timing`，`revisionOf=wave_expiry_b11_takeover_add_v1`。
- 仅覆盖波段抄底型个股日线：已有30%，前一日必须为到期防守观察 `defensive`，当日新触发 `B11`，收盘同时站上 `MA5/MA20`，且 `MA20` 不下行；离场、预警、冷静期、真实失效、风险上限和个股趋势资格继续生效。
- 仓位按“30%基础层＋20%波段层”处理。若核心宽基偏弱且标的仍是普通机会，只对本事件放宽普通机会30%新增风险上限，允许 `30% -> 50%`；不允许 `0% -> 50%`，不允许由本事件继续提高到80%。额外20%先减时，不得连带清空仍满足原观察规则的30%基础层。
- 不改指数、稳健趋势型、突破追涨型、综合全能型、普通B11或主图首次 `B/S`；不全局取消核心宽基偏弱下普通机会30%新增风险上限。

### 快速筛选

- 范围：中青旅定向样本＋16只 `seed` 股票＋8个指数。
- 中青旅 `2026-07-27` 从30%提高到50%，随后1/3/5日收益为 `+0.72% / +2.30% / +4.75%`，5日最大不利走势 `-0.43%`、最大有利走势 `+5.04%`。
- 只有目标事件命中；指数变化0、`B/S` 变化0，状态 `continue_full`。
- 报告：`.local/strategy-reports/wave-expiry-b11-takeover-add-v2-screen-20260809T103126855Z.json`。

### 97只股票＋8个指数全量回放

- 全量只命中2次：中青旅 `2026-07-27` 与金山办公 `2021-10-15`。金山办公随后1/3/5日为 `-3.78% / +3.47% / -0.23%`，5日最大不利走势 `-6.50%`、最大有利走势 `+5.18%`。
- 候选只改变10个仓位决策日；指数变化0、主图 `B/S` 变化0。
- 平均收益变化 `-0.0069` 个百分点，平均最大回撤变化 `+0.0062` 个百分点，换手增加 `0.4`、增幅 `0.0112%`；分别低于收益回退1个百分点、回撤恶化0.5个百分点和换手增幅10%的信号时机预算。
- 状态为 `ready_for_product_review`。报告：`.local/strategy-reports/wave-expiry-b11-takeover-add-v2-full-20260809T104351692Z.json`。

### 作用域补测与结论

- 用中青旅＋8个指数分别复算稳健趋势型、突破追涨型和综合全能型，仓位与 `B/S` 变化均为0；报告：`.local/strategy-reports/wave-expiry-b11-takeover-add-v2-cross-strategy-20260809T104718500Z.json`。
- 该候选解决了目标案例，且组合损害和作用域硬边界均通过；但全量只有2次事件，不能宣称收益优势。
- 影子阶段只保留报告和冻结定义；全量事件数仍只有2次，不宣称收益优势。

### 人工产品决策与生产接入（2026-08-09）

- 用户明确按建议批准接入该窄例外；不修改影子报告中的 `ready_for_product_review` 测量结论，也不全局取消核心宽基偏弱下普通机会30%新增风险上限。
- 生产实现严格使用“30%基础仓＋20%波段仓”双层决策：中青旅 `2026-07-27` 由30%提高到50%，不新增 `B`；`2026-08-07` 额外20%先退出，保留30%进入既有MA20回踩观察，不新增 `S`。普通B11、空仓建仓、80%仓位、指数和其他三套策略保持原规则。
- 右侧结论新增“防守接管加仓 / 波段仓持有 / 波段仓减仓”，明确弱市门禁只是本事件窄放宽，额外20%减仓不等于基础仓失效。
- 本地生产版本提升为 `APP_BUILD=2026-08-09-09`、资源版本 `20260809-09`、`SIGNAL_VERSION=v4.2.28`。
- 验证通过：真实中青旅定向复算、策略决策分组、238项完整回归、41个JavaScript文件语法检查、版本同步检查和一次1440×900桌面状态 smoke；四张图、右侧结论和确认数据状态正常。当前仅本地接入，未提交、未推送、未发布。
