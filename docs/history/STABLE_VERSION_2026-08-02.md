# DailyGlance 稳定记录（2026-08-02）

## 发布版本

- `APP_BUILD=2026-08-02-01`
- 资源参数：`?v=20260802-01`
- `SIGNAL_VERSION=v4.2.13`
- GitHub Pages `main` 提交：`cf02794`
- `Deploy DailyGlance` run `30709291097`、Pages run `30709290637` 均为 `success`
- 在线地址：https://woodslope.github.io/dailyglance/

## 本轮方向调整

- 保留应用的核心决策链：标的信号与风险 -> 核心宽基增量门禁 -> 策略参考仓位/风险仓位 -> 动作与失效条件。
- 核心宽基偏弱时，普通机会新增风险上限 `30%`，标的自身独立走强上限 `50%`；宽基不强制压缩已有仓位，硬风控不受门禁或普通回差阻止。
- 前端删除策略健康清单和“观察级/验证级”展示；成本压力、指数分层和快照证据只作核心策略变更的离线研究工具。
- 删除一次性门禁实验脚本，避免为一次性规则增加长期维护面。

## 验证

- `node tests/regression.js` 通过。
- 相关 JavaScript `node --check` 通过。
- 本地 `status-smoke` 通过：四张图表、右侧结论、确认日期和新资源版本正常。
- 本地 `live-dataflow-smoke` 通过：确认数据、左侧标签、历史拖拽和恢复最新状态正常。
- 线上 smoke 三次尝试均在访问 Pages 时遭遇 `ERR_CONNECTION_RESET`，未进入应用断言阶段；GitHub 两个部署工作流已成功，该项作为当前终端的外部网络风险保留。
- `git diff --check` 通过。

## 后续边界

- 只有新增/删除信号、改积分阈值、改变第一次 `B/S` 或交易频率，或需要宣传收益优势时，才运行成本压力、时间窗口和必要分层的完整回放。
- 普通展示或语义修正只做定向回归，不再因研究策略的收益等级、成本场景或指数分层而阻塞发布。
